# Environment Configuration
# NODE_ENV controls: Swagger docs, CORS origins, rate limits, error details, PayOS mock, logging
# Values: development (default) | production
NODE_ENV="development"

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

# JWT (kept for compatibility, but Firebase tokens will be primary)
JWT_SECRET="your-secret-key-change-in-production"

# Frontend URL for CORS
FRONTEND_URL="http://localhost:5173"

# Firebase Admin SDK Configuration
# Choose ONE of the following options:

# Option 1 (BEST for Vercel/Heroku/Docker): Base64-encoded JSON (避免 \n 問題)
# This avoids issues with newline characters being misinterpreted by deployment platforms
# Steps to generate:
#   Mac/Linux: base64 -i firebase-service-account.json
#   Windows: certutil -encode firebase-service-account.json output.b64 (then copy content)
# FIREBASE_SERVICE_ACCOUNT_BASE64="eyJ0eXBlIjoic2VydmljZV9hY2NvdW50IiwicHJvamVjdF9pZCI6InNpZ21hLWljb24tNDUyODE1LXEyIiwicHJpdmF0ZV9rZXlfaWQiOiIuLi4iLC..."

# Option 2: Full JSON as string (may have issues with \n on some platforms)
# FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"sigma-icon-452815-q2",...}'

# Option 3: Individual fields (easier to read, but more variables)
FIREBASE_PROJECT_ID="sigma-icon-452815-q2"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxxxx@sigma-icon-452815-q2.iam.gserviceaccount.com"

# Option 3: Use service account JSON file (local development only)
# Place the file in apps/backend/ and name it firebase-service-account.json
# FIREBASE_SERVICE_ACCOUNT_PATH="./firebase-service-account.json"

# Admin Configuration
# User registering with this email will automatically become ADMIN
ADMIN_EMAIL="admin@yourdomain.com"

# PayOS Payment Gateway Configuration
# In development: mock mode is enabled by default (set PAYOS_MOCK_MODE=false to disable)
# In production: mock mode is disabled by default (requires real credentials)
# PAYOS_MOCK_MODE="false"  # Uncomment to use real PayOS in development
# For production, set these values from PayOS Dashboard:
# PAYOS_CLIENT_ID="your_client_id"
# PAYOS_API_KEY="your_api_key"
# PAYOS_CHECKSUM_KEY="your_checksum_key"
